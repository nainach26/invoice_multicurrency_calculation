# Required — set a strong random secret before running docker compose
# Generate one with: openssl rand -hex 32
INVOICE_API_KEY=

# Optional — defaults to http://localhost:3000 for local development
# CORS_ALLOWED_ORIGIN=https://your-production-domain.com

# Optional — maximum number of invoice line items allowed (default: 25)
# MAX_INVOICE_LINES=25

